<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/stylesheet/style.css">
</head>
</head>
<body class="w-full min-h-sceen bg-zinc-900"> 
    <div class="p-10">
        <a class="px-3 py-1 bg-blue-800 rounded-md text-white" href="/logout"> logout</a>
        <a class="px-3 py-1 bg-blue-800 rounded-md text-white" href="/profile/uploads"> change profile pic</a>
        <div class="flex items-start gap-5 mt-10 mb-10">
            <div class="w-10 h-10 bg-blue-500 rounded-md overflow-hidden">
                <img class="w-full h-full object-cover hover:object-top" src="/images/uploads/<%= user.profilepic%>" alt="">
            </div>
            <h3 class="text-white text-3xl"> hello, <span class=" rounded-md text-white p-1"><%= user.name%> 👋</span></h3>
        </div>
        <h3 class="py-1 mt-5 mb-5"><span class="px-3 py-1 bg-zinc-600 rounded-md text-white"> you can cretae a new post</span></h3>
        <form class=" flex flex-wrap gap-3" action="/post" method="post">
            <textarea class="resize-none bg-transparent border-2 border-zinc-700 rounded-md w-1/3 py-2 px-1 text-white" name="thoghts" placeholder="write whatever you need to write"></textarea>
            <input class="px-2 py-1 mt-8 bg-blue-800 rounded-md text-white" type="submit" value="submit">
        </form>
        <div class="posts mt-14">
            <h3 class="text-zinc-300">Posts</h3>
            <% user.posts.reverse().forEach(function(post){ %>
                <div class="postcontaier mt-5">
                    <div class="post w-1/2 p-5 border-2 border-zinc-700">
                        <h4 class="text-blue-400">@<%= user.username %></h4>
                        <p class="text-white p-2"><%= post.content %></p>
                        <small class="text-white block mt-5 ml-2"><%= post.likes.length%></small>
                        <div class="btns flex flex-wrap gap-10 ">
                            <a class="text-blue-400" href="/like/<%= post._id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "Like": "Unlike"%>
                            </a>
                            <a class="text-zinc-400" href="/edit/<%= post._id%>">edit</a>
                        </div>  
                    </div>
                </div>
            <%})%>
        </div>
    </div>
</body>
</html>